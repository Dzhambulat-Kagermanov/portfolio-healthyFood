import Info from "../../UI/INFO/Info";
import "./Header.css";

export default function Header(props) {
  return (
    <div
      className="wrapper footer-block__wrapper --header"
      id="contacts-anchor"
    >
      <Info />
    </div>
  );
}
